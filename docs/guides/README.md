# Guides

Task-oriented docs for people using SafeClaw. (The rest of `docs/` is
engineering reference: protocol, schemas, service definitions.)

| Guide | Answers |
|---|---|
| [Quickstart](quickstart.md) | Zero to first brokered call, step by step |
| [`sc run` and phantoms](sc-run.md) | How an agent uses a key it can't see, and the `sc get` anti-pattern |
| [Security model](security-model.md) | Where keys live, what the cloud can and cannot see, what a compromised agent can do |
| [For your agent](for-your-agent.md) | Handing SafeClaw to an agent: the skill file and a paste-ready prompt |

Hitting an error? Every code the broker can surface is catalogued in
[DIAGNOSTICS.md](../DIAGNOSTICS.md).
