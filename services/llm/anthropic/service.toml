[service]
id = "anthropic"
name = "Anthropic"
group = "anthropic"

hosts = ["api.anthropic.com"]
secrets = ["ANTHROPIC_API_KEY"]
# Where a human mints the secret (auxiliary; display-only helper link).
secret_url = "https://console.anthropic.com/settings/keys"
