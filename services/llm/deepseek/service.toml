[service]
id = "deepseek"
name = "DeepSeek"

hosts = ["api.deepseek.com"]
secrets = ["DEEPSEEK_API_KEY"]
# Where a human mints the secret (auxiliary; display-only helper link).
secret_url = "https://platform.deepseek.com/api_keys"
