[service]
id = "deepseek"
name = "DeepSeek"

hosts = ["api.deepseek.com"]
secrets = ["DEEPSEEK_API_KEY"]
# Where a human mints the token (auxiliary; console/CLI helper link).
key_page = "https://platform.deepseek.com/api_keys"
