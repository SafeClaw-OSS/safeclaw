[service]
id = "openai"
name = "OpenAI"
group = "openai"

hosts = ["api.openai.com"]
secrets = ["OPENAI_API_KEY"]
# Where a human mints the secret (auxiliary; display-only helper link).
secret_url = "https://platform.openai.com/api-keys"
