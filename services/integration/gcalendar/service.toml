[service]
id = "gcalendar"
name = "Google Calendar"

hosts = ["www.googleapis.com"]
secrets = ["GOOGLE_CALENDAR_REFRESH_TOKEN"]

[oauth2]
provider = "google"
scopes = [
  "https://www.googleapis.com/auth/calendar.events",
]
refresh_token = "GOOGLE_CALENDAR_REFRESH_TOKEN"
